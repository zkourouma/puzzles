name: sum-of-multiples
version: 1.4.1.8

dependencies:
  - base

library:
  exposed-modules: SumOfMultiples
  source-dirs: src
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - sum-of-multiples
      - hspec
